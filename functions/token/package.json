{
  "name": "token",
  "version": "1.0.0",
  "description": "Netlify function for fetching authentication tokens from GitHub during the OAuth flow",
  "main": "token.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^2.6.0"
  }
}
